name: Deploy to GitHub Pages

on:
  push:
    branches:
      - master  # o master, según la rama principal que uses

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3

      - name: Setup Node.js ⚙️
        uses: actions/setup-node@v3
        with:
          node-version: 18  # Usa la versión de Node que prefieras
          cache: 'npm'

      - name: Install dependencies 📦
        run: npm ci

      - name: Build 🔧
        run: npm run build

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: out  # El directorio de salida generado por Next.js
          branch: gh-pages  # La rama donde se desplegará el sitio